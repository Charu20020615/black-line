# Backend Environment Variables
# Copy this file to .env in the backend folder or set in Railway dashboard

PORT=4000
NODE_ENV=production

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-domain.railway.app

# Railway Specific (auto-set by Railway, but can override)
RAILWAY_PUBLIC_DOMAIN=https://your-backend.railway.app
RAILWAY_STATIC_URL=https://your-frontend.railway.app

# Frontend Environment Variables (for Vite)
# Set this in your frontend deployment (Vercel/Netlify/etc)
# VITE_API_BASE_URL=https://your-backend.railway.app

